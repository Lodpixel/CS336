# tokenization 部分总结

主要是python语法熟悉度的问题，好久没写py，有点手生，不熟悉的api很多，debug里有很多小问题吧

核心难点在于各种类型的切换，从 gpt2，utf-8，bytes，int，这些长得奇形怪状的类型还有各处的转换很麻烦

有一个优化点就是一开始我测试最后一个一直卡死，然后发现我的merge操作逻辑，对每一个分割块，遍历了所有的merge logic，但事实上我们更应该遍历这个块的位置，然后进行一步步merge，将merge转换成一个dict，可以大大提高我们访问的速度。

